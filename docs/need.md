# 个人专属生活助理 Agent 需求文档 V1.0

## 一、项目概述

本项目旨在构建一个基于大语言模型（LLM）的个人生活助理 Agent。系统部署在 Cloudflare Workers 上，使用 Supabase 作为数据中心，最终希望让用户在微信生态中通过聊天随时调用 Agent。

重要结论：如果目标是“像给普通微信好友发消息一样，在个人微信聊天列表里直接和 Agent 对话，并且 Agent 可以随时主动推送任意提醒”，官方稳定方案目前不可行。个人微信没有面向个人号开放的机器人收发消息 API；非官方个人号协议、模拟登录、Hook、逆向方案存在封号和长期不可维护风险，V1 不采用。

因此，本项目采用“通道适配器”设计：核心 Agent 能力与消息通道解耦。明确版本路线如下：

* **V1：** 先用企业微信自建应用实现完整能力，包括消息接收、主动回复、备忘录、防忘提醒、网页总结、天气和搜索。
* **V1.5：** 增加微信公众号通道，作为个人微信 App 内的聊天入口。用户可以在个人微信里打开公众号会话，通过聊天调用 Agent。
* **公众号提醒降级：** 公众号侧提醒能力必须按官方客服消息、订阅消息或模板消息规则降级，不能承诺任意时间主动发送任意文本提醒。

第一版目标不是一次性做成完整个人操作系统，而是先跑通一个稳定、低成本、可长期迭代的生活助理底座。V1 优先支持以下能力：

1. 企业微信消息接入、异步处理与主动回复。
2. 智能备忘录与“防忘”提醒。
3. 碎片信息“消化器”：网页链接读取与总结。
4. 天气查询。
5. 联网信息获取与整理。

## 二、关键约束与边界

### 1. 微信接入边界

V1 的“微信接入”指通过**企业微信自建应用**实现消息接收和主动推送。实现上使用企业微信后台的接收消息服务器配置、消息加解密机制、自建应用消息发送 API。

V1 不接入个人微信非官方协议，不做模拟登录、Hook、逆向协议或任何可能导致微信账号风险的方案。

V1.5 增加**微信公众号通道**，用于满足“在个人微信 App 内通过聊天调用 Agent”的目标：

* 用户可以在个人微信里打开公众号会话并发送消息给 Agent。
* Agent 可以在用户主动交互后的客服消息窗口内回复。
* 超出官方允许窗口后，公众号不能随意主动发送任意文本提醒。
* 长周期提醒需要改造成官方允许的模板/订阅消息形态，且可能需要用户授权或点击订阅。
* 如果公众号无法按规则发送某条提醒，应在企业微信通道保留强提醒能力，并在提醒记录中标记公众号侧发送受限。

因此，V1 将完整能力优先放在企业微信通道；V1.5 用公众号承接个人微信聊天入口；提醒能力在公众号侧按官方订阅/模板消息限制做降级。

### 2. 成本约束

除 LLM API 调用成本之外，V1 应尽量做到接近 0 成本：

* Cloudflare Workers 使用免费额度。
* Supabase 使用免费额度。
* Jina Reader 优先作为网页正文提取能力。
* 天气 API 优先选择可免费调用的服务。
* 搜索能力优先使用免费方案或免费额度，Tavily 仅作为可选增强。
* 对 LLM 调用进行节制：能用规则判断的路由不要交给 LLM；长网页必须截断；搜索结果只取 Top 3；提醒确认尽量短回复。

### 3. V1 暂不做

* 多用户复杂权限系统。
* 日历同步、邮件同步、文件管理。
* 语音、图片、多模态输入。
* 长期记忆画像和复杂 RAG 知识库。
* 面向公网多用户注册的 SaaS 化能力。

## 三、核心技术栈

* **计算与网关：** Cloudflare Workers，使用 TypeScript 与 ES Modules。
* **定时任务：** Cloudflare Workers Cron Triggers，用于扫描并触发提醒。
* **数据库：** Supabase PostgreSQL，用于存储提醒、备忘录、任务状态和必要的会话信息。
* **V1 微信通道：** 企业微信自建应用，用于完整能力和强提醒。
* **V1.5 微信通道：** 微信公众号，用作个人微信 App 内的聊天入口，但主动提醒能力按官方规则降级。
* **LLM API：** DeepSeek / Kimi / OpenAI 兼容格式接口。
* **网页解析：** Jina Reader API (`https://r.jina.ai/`)。
* **搜索引擎：** 免费搜索方案优先，Tavily API 可选。
* **天气查询：** 和风天气 / OpenWeather / 其他免费额度天气 API。

## 四、底层架构设计（CodeX 必读）

### 1. 企业微信 Webhook 鉴权机制

企业微信后台配置接收消息服务器时，会发送 GET 请求进行 URL 验证。

实现要求：

1. 接收 `msg_signature`、`timestamp`、`nonce`、`echostr` 参数。
2. 使用配置的 `WX_TOKEN` 和 `WX_ENCODING_AES_KEY` 校验签名并解密 `echostr`。
3. 原样返回解密后的明文，完成 URL 验证。

### 2. 5 秒防超时与异步通信模型

企业微信 Webhook 必须快速响应，不能等待 LLM、搜索、网页读取等慢任务完成。

实现要求：

1. 收到 POST 请求后，解析并解密 XML 消息，提取用户 ID 与消息文本。
2. 使用 `ctx.waitUntil(handleTask())` 启动后台异步任务。
3. 主请求立即返回 HTTP 200 或空字符串。
4. `handleTask()` 中执行路由、工具调用、LLM 生成和结果组装。
5. 结果生成后，调用企业微信发送应用消息 API，主动推送纯文本结果给用户。

### 3. 主动消息发送

发送消息前需要获取企业微信 `access_token`。V1 可以先简单请求 token；后续再加入缓存，避免每次消息都请求 token。

## 五、V1 实现顺序

### 阶段 1：企业微信接入骨架

目标：先证明消息链路完整可用。

必须实现：

* GET URL 验证。
* POST 消息接收、解密与基础解析。
* 5 秒内立即响应。
* 后台异步任务中主动发送一条测试回复。
* 所有敏感配置从环境变量读取。

验收标准：

* 企业微信后台 URL 验证通过。
* 用户发送任意文本后，系统能主动回复。
* 慢任务不会导致 Webhook 超时。

### 阶段 2：智能备忘录与“防忘”提醒

目标：做出最有个人助理价值的第一项生活能力。

用户示例：

* “明早 8 点提醒我带护照”
* “今晚 10 点提醒我交电费”
* “下周一上午 9 点提醒我开周会”

处理流程：

1. Router 判断消息是否为提醒/备忘录意图。
2. 提取提醒内容和目标时间。
3. 目标时间默认使用 `Asia/Shanghai` 时区。
4. 写入 Supabase `reminders` 表。
5. 立即回复用户设置结果。
6. Cloudflare Cron 每分钟扫描待发送提醒。
7. 到点后主动推送提醒，并更新发送状态。

推荐数据结构：

* `id` (uuid)
* `user_id` (text)
* `content` (text)
* `source_message` (text)
* `target_time` (timestamptz)
* `timezone` (text, 默认 `Asia/Shanghai`)
* `status` (text: `pending` / `sent` / `failed` / `cancelled`)
* `retry_count` (integer, 默认 0)
* `created_at` (timestamptz)
* `updated_at` (timestamptz)
* `sent_at` (timestamptz, nullable)

验收标准：

* 能正确创建未来提醒。
* Cron 到点后能推送提醒。
* 已发送提醒不会重复发送。
* 时间无法解析时，主动要求用户补充具体时间。

### 阶段 3：碎片信息“消化器”（网页链接总结）

目标：用户转发链接后，Agent 自动读取网页并总结。

触发条件：

* 用户消息包含合法的 `http://` 或 `https://` 链接。

处理流程：

1. 提取消息中的第一个 URL。
2. 拼接 Jina Reader 地址，例如 `https://r.jina.ai/http://example.com` 或 `https://r.jina.ai/http://r.jina.ai/http://example.com` 的错误嵌套必须避免。
3. 获取 Markdown/纯文本正文。
4. 截断到模型上下文允许范围内。
5. 调用 LLM 生成 3-5 个核心要点和一段简短总结。
6. 主动推送总结给用户。

验收标准：

* 常见文章链接可成功总结。
* 读取失败时能给出明确失败提示。
* 超长网页不会导致 LLM 请求过大。

### 阶段 4：天气查询

目标：支持自然语言天气查询，并给出生活建议。

用户示例：

* “明天上海天气怎么样？”
* “今天出门要带伞吗？”
* “周末杭州冷不冷？”

处理流程：

1. Router 判断天气意图。
2. 提取城市和日期；缺城市时可默认使用用户配置城市，V1 未配置时要求用户补充。
3. 调用天气 API。
4. 将天气数据整理成简短、可行动的中文回复。

验收标准：

* 能查询指定城市当天/未来天气。
* 能输出温度、天气状况、降雨/带伞建议。
* API 失败时能返回友好错误。

### 阶段 5：联网信息获取与处理

目标：减少用户手动搜索和筛选信息的成本。

触发条件：

* 用户明确要求“查一下”“搜一下”“网上怎么说”。
* 用户问题涉及最新信息、时效事实或外部事实补充。

处理流程：

1. Router 生成搜索关键词。
2. 调用免费搜索方案或 Tavily API 获取 Top 3 结果。
3. 提取标题、摘要、链接和必要片段。
4. LLM 综合回答，并附上来源链接。
5. 对不确定结论明确标注“不确定”或“根据当前搜索结果”。

验收标准：

* 能回答需要联网的信息问题。
* 回复中包含简短来源。
* 搜索失败时不会编造答案。

## 六、Router 路由策略

V1 优先使用规则路由，减少 LLM 成本和不确定性。建议顺序如下：

1. 如果消息包含 URL，优先进入网页总结。
2. 如果包含“提醒我”“记得”“到时候”“明天/今晚/下周 + 时间”等表达，进入提醒解析。
3. 如果包含“天气”“下雨”“带伞”“冷不冷”“热不热”等表达，进入天气查询。
4. 如果包含“查一下”“搜一下”“最新”“网上怎么说”等表达，进入联网搜索。
5. 其他消息进入普通 LLM 对话。

对于时间解析、搜索关键词生成、复杂自然语言理解，可以调用 LLM；但简单触发判断应优先使用规则。

## 七、环境变量清单

### 企业微信配置

* `WX_CORP_ID`：企业 ID
* `WX_APP_SECRET`：自建应用 Secret
* `WX_AGENT_ID`：自建应用 ID
* `WX_TOKEN`：接收消息服务器 Token
* `WX_ENCODING_AES_KEY`：接收消息 EncodingAESKey

### LLM 与外部 API

* `LLM_API_KEY`：大模型 API Key
* `LLM_BASE_URL`：大模型 Base URL
* `LLM_MODEL`：模型名称
* `TAVILY_API_KEY`：搜索 API Key，可选
* `WEATHER_API_KEY`：天气 API Key

### 数据库配置

* `SUPABASE_URL`：Supabase 项目 URL
* `SUPABASE_SERVICE_ROLE_KEY`：服务端使用的 Supabase service role key

注意：`SUPABASE_SERVICE_ROLE_KEY` 只能放在 Cloudflare Workers 服务端环境变量中，不能暴露到前端或日志中。

## 八、错误处理与安全要求

* 所有外部 API 调用必须设置超时。
* LLM、搜索、天气、网页读取失败时，应返回可理解的中文错误提示。
* 微信消息解密失败或签名不匹配时，必须拒绝处理。
* 日志中不得输出完整密钥、access token 或 Supabase service role key。
* 提醒发送失败时，记录 `failed` 或增加 `retry_count`，避免无限重复发送。
* Webhook 主请求不得等待慢任务完成。

## 九、建议的代码生成步骤

1. 先实现企业微信 Webhook 骨架：GET 验证、POST 解密、异步处理、主动发送测试消息。
2. 接入 Supabase，创建 `reminders` 表，实现提醒写入和 Cron 扫描发送。
3. 实现 Router 的规则分发。
4. 实现网页链接总结。
5. 实现天气查询。
6. 实现联网搜索。
7. 将消息通道抽象成 Adapter，至少包含 `receiveMessage`、`sendMessage`、`verifyWebhook` 三类能力，避免后续从企业微信迁移到公众号时重写核心 Agent。
8. 最后补充失败处理、token 缓存、日志清理和基础测试。

## 十、长期个人微信入口方案

长期目标采用“双通道”策略，而不是试图把个人微信号变成机器人。

### V1：企业微信完整能力通道

企业微信自建应用作为第一阶段主入口，用于实现完整个人助理能力：

* 接收用户消息。
* 主动推送提醒。
* 承载备忘录、防忘提醒、网页总结、天气、搜索等完整功能。
* 保证提醒能力不受公众号客服消息窗口限制。

### V1.5：公众号个人微信聊天入口

微信公众号作为第二阶段新增通道，用于满足“在个人微信 App 内通过聊天调用 Agent”的体验：

* 用户在个人微信里打开公众号会话并发送消息。
* 公众号 Webhook 将消息转给同一套 Agent Core。
* Agent Core 返回结果后，通过公众号客服消息在允许窗口内回复。
* 天气、网页总结、搜索、普通问答优先支持公众号入口。

### 公众号提醒降级规则

公众号通道不能作为唯一强提醒通道。提醒能力按以下规则降级：

* 用户刚与公众号互动且仍在官方允许窗口内时，可以尝试通过公众号客服消息提醒。
* 长周期提醒优先使用企业微信通道发送。
* 如果后续接入订阅消息或模板消息，只发送符合官方模板和授权规则的提醒。
* 公众号发送受限时，不视为核心提醒失败；企业微信强提醒成功即可视为提醒任务完成。
* 数据库中应保留提醒的通道发送状态，便于判断企业微信是否成功、公众号是否受限。

### 不采用方案

不使用个人微信非官方机器人方案，包括但不限于模拟登录、网页版微信残留协议、Hook 客户端、逆向私有接口。原因是账号风险高、稳定性差、维护成本不可控，与“长期个人助理”目标冲突。
