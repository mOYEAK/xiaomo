{
  "name": "personal_agent",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:verify": "tsc --outDir dist/verify --rootDir src --noEmit false && node scripts/patch-verify-imports.mjs",
    "verify:agent": "npm run build:verify --silent && node scripts/verify-agent.mjs",
    "verify:llm": "npm run build:verify --silent && node scripts/verify-llm.mjs",
    "verify:reminders": "npm run build:verify --silent && node scripts/verify-reminders.mjs",
    "verify:web": "npm run build:verify --silent && node scripts/verify-web-reader.mjs",
    "verify:mp": "node scripts/verify-mp.mjs",
    "verify:wecom": "node scripts/verify-wecom.mjs",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "crypto-js": "^4.2.0",
    "fast-xml-parser": "^5.8.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260531.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.96.0"
  }
}
